spring:
  datasource:
    url: jdbc:mysql://localhost:3306/house_price_db?createDatabaseIfNotExist=true
    username: root
    password: Likhith@2004
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQL8Dialect
        format_sql: true
        jdbc.batch_size: 50
    defer-datasource-initialization: true
  sql:
    init:
      mode: always
      continue-on-error: true

ml:
  service:
    url: http://localhost:8001

server:
  port: 8080
